Place the supplied workbook here as `UseCase_Airlines.xlsx`.

The workbook contains personal data and is excluded from Git. The local copy is byte-for-byte identical to `UseCase - Airlines.xlsx`. Share it only through the assessment's approved private channel. A clean checkout needs this file and `ASG_PII_PEPPER` before execution.

